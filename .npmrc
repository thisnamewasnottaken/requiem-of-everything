# Only fail CI audits on critical severity vulnerabilities.
# High-severity issues in dev-only dependencies (e.g. flatted in @vitest/ui)
# are noted but do not block builds. Remove once upstream ships the fix.
audit-level=critical
